import { RegisterForm } from "@/components/auth/register-form";

export default function CadastroPage() {
  return (
    <main className="flex flex-1 items-center justify-center bg-page px-4 py-10">
      <RegisterForm />
    </main>
  );
}
